import data from '../../data/Questions.json'

// initial state
const state = () => ({
  questions: data
})

// mutations
const mutations = {
}

// actions
const actions = {
}

export default {
  state,
  actions,
  mutations
}
